// NEMESIS AI COMMAND CENTER - COMPLETE DASHBOARD
import React, { useState } from 'react';
import { useDashboardData } from '../hooks/useDashboardData';
import { ErrorBoundary } from '../components/common/ErrorBoundary';

// Real Components
import { AIStatusBar } from '../components/intelligence/AIStatusBar';
import { AIRiskScore } from '../components/intelligence/AIRiskScore';
import { IntelligenceKPICards } from '../components/intelligence/IntelligenceKPICards';
import { GraphIntelligence } from '../components/graph/GraphIntelligence';
import { RiskReasoningPanel } from '../components/investigation/RiskReasoningPanel';
import { DecisionSupport } from '../components/decision/DecisionSupport';
import { FraudPatternDetection } from '../components/fraud/FraudPatternDetection';
import { EvidenceChainOfCustody } from '../components/evidence/EvidenceChainOfCustody';
import { InvestigatorQueue } from '../components/investigator/InvestigatorQueue';
import { AICopilot } from '../components/copilot/AICopilot';
import { StrategicRiskMap } from '../components/risk-map/StrategicRiskMap';

// Placeholders until real components are created
const EvidenceIntelligence = () => <div className="glass-card p-4">Evidence Intelligence</div>;

export const Dashboard: React.FC = () => {
  const [selectedCaseId] = useState('446e216d-eb0e-487e-8e6b-ec943468ea20');
  const [selectedEvidenceId] = useState('abc1035e-c182-49c1-88ad-9019720fb2e2');
  const { loading, error, data } = useDashboardData(selectedCaseId);

  if (loading) {
    return (
      <div className="flex items-center justify-center h-screen bg-dark-bg">
        <div className="text-center animate-fade-in-up">
          <div className="animate-spin rounded-full h-16 w-16 border-4 border-primary-500 border-t-transparent mx-auto mb-4" />
          <p className="text-dark-muted text-lg font-medium">Loading NEMESIS AI Command Center...</p>
          <p className="text-dark-muted/60 text-sm mt-2">Initializing intelligence systems</p>
        </div>
      </div>
    );
  }

  if (error) {
    return (
      <div className="flex items-center justify-center h-screen bg-dark-bg">
        <div className="bg-red-500/10 border border-red-500/30 rounded-xl p-6 max-w-md text-center">
          <AlertTriangle className="w-12 h-12 text-red-400 mx-auto mb-4" />
          <p className="text-red-400 font-semibold text-lg">System Error</p>
          <p className="text-red-400/70 mt-2">{error}</p>
          <button 
            onClick={() => window.location.reload()}
            className="mt-4 px-4 py-2 bg-red-500/20 hover:bg-red-500/30 rounded-lg text-red-400 transition-colors"
          >
            Retry
          </button>
        </div>
      </div>
    );
  }

  const { executive, cases, evidence, keyActors } = data;

  const handleInvestigate = (patternId: string) => {
    console.log('🔍 Investigating pattern:', patternId);
  };

  return (
    <div className="min-h-screen bg-dark-bg text-dark-text animate-fade-in">
      <AIStatusBar data={{ executive, cases, evidence, keyActors }} />

      <main className="max-w-7xl mx-auto px-4 py-6 space-y-6">
        {/* Row 1: AI Risk Score + KPI Cards */}
        <div className="grid grid-cols-1 lg:grid-cols-5 gap-4">
          <div className="lg:col-span-1 animate-slide-up" style={{ animationDelay: '0.1s' }}>
            <AIRiskScore riskScore={executive?.risk_level?.score || 0} />
          </div>
          <div className="lg:col-span-4 animate-slide-up" style={{ animationDelay: '0.2s' }}>
            <IntelligenceKPICards data={{ executive, cases, evidence, keyActors }} />
          </div>
        </div>

        {/* Row 2: Network Graph - Full Width */}
        <div className="animate-slide-up" style={{ animationDelay: '0.3s' }}>
          <ErrorBoundary fallback={<div className="text-red-500 p-4">Graph Error</div>}>
            <GraphIntelligence actors={keyActors} />
          </ErrorBoundary>
        </div>

        {/* Row 3: Risk Reasoning + Fraud Detection */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-4">
          <div className="animate-slide-up" style={{ animationDelay: '0.4s' }}>
            <ErrorBoundary fallback={<div className="text-red-500 p-4">Risk Reasoning Error</div>}>
              <RiskReasoningPanel 
                caseId={selectedCaseId}
                onInvestigate={(id) => console.log('Investigating:', id)}
                onGenerateReport={(id) => console.log('Generating report:', id)}
              />
            </ErrorBoundary>
          </div>
          <div className="animate-slide-up" style={{ animationDelay: '0.5s' }}>
            <ErrorBoundary fallback={<div className="text-red-500 p-4">Fraud Pattern Detection Error</div>}>
              <FraudPatternDetection onInvestigate={handleInvestigate} />
            </ErrorBoundary>
          </div>
        </div>

        {/* Row 4: Decision Support + Evidence Chain */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-4">
          <div className="animate-slide-up" style={{ animationDelay: '0.6s' }}>
            <ErrorBoundary fallback={<div className="text-red-500 p-4">Decision Support Error</div>}>
              <DecisionSupport caseId={selectedCaseId} />
            </ErrorBoundary>
          </div>
          <div className="animate-slide-up" style={{ animationDelay: '0.7s' }}>
            <ErrorBoundary fallback={<div className="text-red-500 p-4">Evidence Chain Error</div>}>
              <EvidenceChainOfCustody evidenceId={selectedEvidenceId} />
            </ErrorBoundary>
          </div>
        </div>

        {/* Row 5: AI Copilot + Investigator Queue */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-4">
          <div className="animate-slide-up" style={{ animationDelay: '0.8s' }}>
            <ErrorBoundary fallback={<div className="text-red-500 p-4">Copilot Error</div>}>
              <AICopilot />
            </ErrorBoundary>
          </div>
          <div className="animate-slide-up" style={{ animationDelay: '0.9s' }}>
            <ErrorBoundary fallback={<div className="text-red-500 p-4">Queue Error</div>}>
              <InvestigatorQueue cases={cases} recommendations={[]} />
            </ErrorBoundary>
          </div>
        </div>
      </main>

      {/* Footer */}
      <footer className="border-t border-dark-border mt-6 py-4 text-center">
        <p className="text-xs text-dark-muted">
          NEMESIS AI v8.0 — Strategic Intelligence Center · All Rights Reserved
        </p>
      </footer>
    </div>
  );
};

export default Dashboard;
