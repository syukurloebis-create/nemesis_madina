# Tambahkan import di bagian atas file
from backend.core.hash.unified_hasher import compute_event_hash, get_previous_hash_from_db

# Update fungsi create_case, di bagian setelah event_data dibuat:

        # Get previous hash for this case
        previous_hash = await get_previous_hash_from_db(db, case_id)
        
        # Compute event hash
        event_hash = compute_event_hash(
            case_id=case_id,
            event_id=event_id,
            event_type="case_created",
            version=version,
            data=event_data,
            previous_hash=previous_hash
        )
        
        # Create event with hash
        event = Event(
            id=event_id,
            case_id=case_id,
            event_type="case_created",
            event_data=event_data,
            event_hash=event_hash,
            previous_hash=previous_hash,
            version=version,
            created_by=user_id
        )
